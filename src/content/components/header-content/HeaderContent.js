import React from 'react';
import { Header, TitleSection } from '../../../App.styled';

const HeaderContent = () => {
    return (
        <Header>
            <TitleSection>Nodes Navigation Focus</TitleSection>
        </Header>
    );
};

export default HeaderContent;